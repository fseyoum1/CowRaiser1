<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.jkim3213.cowraiser">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".VegetarianChallenge"></activity>
        <activity android:name=".LightChallenge"></activity>
        <activity android:name=".AltTransportChallenge"></activity>
        <activity android:name=".ShowerChallenge"></activity>
        <activity android:name=".RiceChallenge"></activity>
        <activity android:name=".PublTransportChallenge"></activity>


    </application>

</manifest>